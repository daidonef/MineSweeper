package com.daidone.minesweeper;

public class CheckingTable {
	
	//Method to check if player wins, loses, or keeps going
		//Parameters are session and request
		//Uses for loop to check value of 2d array with numbers
		//Returns value for either win, lose, or keep going (so numbers 0, 1, or 2)
	
	//Method to check for blank squares and uses that to show more squares next to 
		//blank squares.
		//Parameters are session and request
		//Uses for loop to check if the value of the 2d array with numbers is blank
		//Returns boolean

}
